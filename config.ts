// config.ts
const API_CONFIG = {
    host: "localhost",
    port: 5001,
  };
  
  export const API_BASE_URL = `http://${API_CONFIG.host}:${API_CONFIG.port}`;

  export default API_CONFIG;
  